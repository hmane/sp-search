# Release run — v1.0.0-rc.1

**Tag commit:** PENDING — Phase C / squash-merge to main not yet executed
**Branch:** feat/spfx-1.22-heft-migration → main (PENDING)
**Run dates:**
- Phase A initial: 2026-05-02 (commit `b17199a`)
- Phase B automated re-run: 2026-05-03 (commit `6725272`)
- Phase B audit re-run: 2026-05-03 (post-`a6e7a8d` audit, this entry)
**Operator:** Hemant Mane
**Phase:** A (automated steps 1-5) **PASS**; B (manual smoke) **PENDING**; C (squash + tag + Release) **PENDING**

## Pre-flight

- [x] Working tree clean (`git status --short` empty)
- [x] Node 22.14.x verified
- [x] On `feat/spfx-1.22-heft-migration`
- [x] HEAD is `a6e7a8d` (last commit on branch — D2 Phase B prep + post-audit fixes will be appended)

## Steps

| # | Step | Outcome | Evidence |
|---|------|---------|----------|
| 1 | `npm install` | SKIP — node_modules already populated; package-lock.json + package.json unchanged since HEAD | `git diff --quiet HEAD package-lock.json package.json` exit 0 |
| 2 | `npm run type-check` | PASS | `tsc --noEmit -p tsconfig.json` exit 0; clean type-check, no output. Found.D3 (`00618bd`) replaced the broken `heft --lite` invocation. |
| 3 | `npm test` | PASS | 83 specs passed across 9 suites; 0 failures; exit 0. Suites: lifecycle (2), pnpPropertyControlsFix (3), Registry (19), configValidation (19), SearchCoverageService (9), storeRegistry (13), safeNavigate (9 — +2 vbscript / protocol-relative cases added during the audit), TelemetryTransport (4), smokeAxe (5). |
| 4 | `npm run package` | PASS | `sharepoint/solution/sp-search.sppkg` produced; **2,632,850 bytes** (~2.6 MB); SHA-256 `18509232c19ee970b7d6254df74bad380c447525ec958a36a1be10a72c40501d` (post-audit final-verification rebuild at HEAD `852fb6f`). Manifest verified: `mpnId=""`, `webApiPermissionRequests=[{resource: "Microsoft Graph", scope: "People.Read"}]`, no `Undefined-1.21` strings present. **Note:** `.sppkg` SHA-256 is non-deterministic across rebuilds even with identical source (heft `package-solution` embeds zip-entry timestamps); expect ±1-byte size drift and a fresh SHA on each `npm run package`. The post-`a6e7a8d` audit pass added the `package-solution.json:longDescription` "five → six" fix (commit `daced79`); webpack web-part chunk hashes were unchanged by that doc-only fix. |
| 5 | `npm run check:bundles` | PASS | All 6 production-hashed web parts within budget at 62.7-66.4% (per Found.D7 amendment commit `63b4cc1` made the gate hash-aware). Per-web-part: filters 862,756 / 1,300,000 (66.4%); results 714,753 / 1,100,000 (65.0%); admin-manager 592,956 / 900,000 (65.9%); manager 592,469 / 900,000 (65.8%); box 548,232 / 850,000 (64.5%); verticals 470,416 / 750,000 (62.7%). |
| 6 | Tenant upload smoke | **PENDING — requires Hemant/manual tenant app catalog upload and browser console verification** | Manual checklist below |
| 7 | Multi-context smoke | **PENDING — requires Hemant/manual tenant page validation** | Manual checklist below |

## Step 6 — Manual checklist (Tenant upload smoke)

Run from a workstation authenticated to the test tenant.

- [ ] **a. Upload `.sppkg` to app catalog**
  Upload `sharepoint/solution/sp-search.sppkg` to the tenant app catalog (or to `https://pixelboy.sharepoint.com/sites/SPSearch` site collection app catalog if scoped). When the SP admin center prompts to trust the app, accept.
  - Reference fingerprint as of HEAD `852fb6f` audit rebuild: 2,632,850 bytes; SHA-256 `18509232c19ee970b7d6254df74bad380c447525ec958a36a1be10a72c40501d`. If you rebuild before upload, capture the fresh fingerprint in the line below — `.sppkg` SHA-256 is non-deterministic across rebuilds (see Step 4 note).
  - Local fingerprint at upload: `__________` bytes; SHA-256 `__________`.
- [ ] **b. Approve Microsoft Graph `People.Read` in SharePoint admin center API access**
  Navigate to SharePoint admin center → Advanced → API access. The pending request `Microsoft Graph: People.Read` should appear (Found.D10 declaration in `config/package-solution.json`). Approve it. Without this approval, the Graph-backed People vertical silently no-ops.
- [ ] **c. Add all 6 web parts to a test page**
  Open a SharePoint page in edit mode, add all 6 web parts (SP Search Box, Results, Filters, Verticals, Manager, Admin Manager). Configure each with the same `searchContextId` (e.g. `smoke-rc1`).
- [ ] **d. Run a basic query**
  Type a query into the Search Box. Confirm the Results web part renders ≥1 result and Filters surfaces refiners.
  - Query used: `__________`
  - Result count rendered: `__________`
- [ ] **e. Verify `?debug=1` opens the DebugFab**
  Append `?debug=1` to the page URL and reload. Confirm the floating Debug FAB appears bottom-right; clicking it opens the Debug Panel.
- [ ] **f. Record browser console state**
  With DevTools open, walk the page (search → filter → switch verticals → open detail panel → close). Record whether any error-level entries appeared in the console.
  - Console errors observed: `none` / `<list error messages>`
  - Console warnings of note (optional): `__________`

Record outcomes (per checkbox: PASS / FAIL <reason> / SKIP <reason>) inline below this list when run.

## Step 7 — Manual checklist (Multi-context smoke)

Run from the same authenticated workstation.

- [ ] **a. Provision/run the multi-context test page**
  The `Provision-TestPages.ps1` script ships a `test-multi-context` page specifically for this scenario. Example invocation (use only if the page is not already present in the test tenant):

  ```powershell
  .\scripts\Provision-TestPages.ps1 `
    -SiteUrl "https://pixelboy.sharepoint.com/sites/SPSearch" `
    -ClientId "970bb320-0d49-4b4a-aa8f-c3f4b1e5928f" `
    -PagesOnly "test-multi-context"
  ```

  Or use an existing multi-context page that places two independent search contexts on the same page (e.g. `searchContextId="ctx1"` and `searchContextId="ctx2"` web part groups).
  - Page URL used: `__________`
  - Page provenance: `provisioned by Provision-TestPages.ps1` / `pre-existing test page`
- [ ] **b. Verify two contexts have isolated query/filter state**
  Type a query in the `ctx1` Search Box and apply a filter in the `ctx1` Filters web part. Confirm `ctx2`'s query and filter state remain unchanged.
- [ ] **c. Verify URL params do not cross-contaminate contexts**
  Inspect the URL after applying state to `ctx1`. Params should be namespaced (e.g. `?ctx1.q=foo&ctx1.f=bar`); `ctx2.*` params should remain absent or empty.
- [ ] **d. Verify navigation away/back does not corrupt state**
  Navigate away (e.g. click into a result or another SharePoint page), then use browser back. Both contexts should restore from URL params with the original state intact, not bleed into each other.

Record outcomes (per checkbox: PASS / FAIL <reason> / SKIP <reason>) inline below this list when run.

## Phase split rationale

Phase A (automated steps 1-5) executes the deterministic checklist steps that can be driven without authenticated tenant access. Phase B (Steps 6-7) requires a SharePoint admin browser session and is owned by the user. Phase C (squash-merge to `main` + tag `v1.0.0-rc.1` + GitHub Release publish) is deferred until Phase B is complete and signed off.

## Resolved concerns from prior Phase A run (2026-05-02)

The 2026-05-02 Phase A run (commit `b17199a`) flagged two concerns:

1. **Bundle-gate hash mismatch** — the gate looked up unhashed filenames but Heft `--production` emits hashed names. **Resolved by Task 3.5** (Found.D7 amendment, commit `63b4cc1`): regex `^<stem>_[a-f0-9]{8,}\.js$`, strict-production. Step 5 above now PASS via the actual gate, no manual workaround needed.
2. **Baseline magnitude vs current** — baseline was 7.96–14.75 MB but actual production sizes are 470K–862K (~17× smaller). **Resolved by Task 3.5**: budgets re-anchored to 1.5× actual production sizes; baseline re-snapshotted at `docs/performance/bundle-sizes-baseline.json`. Found.D12 (active reduction sweep) reclassified to Defer per audit Part 3 Amendment 1 in commit `1174b09`.

## Phase A initial commit

The Phase A run log scaffold was committed alongside `docs/release-smoke-checklist.md` and the narrow CLAUDE.md edits in commit `b17199a` (2026-05-02). This document supersedes the initial Phase A entries with refreshed evidence captured against the post-Task-3.5, post-Tranche-2 HEAD `6725272`.
