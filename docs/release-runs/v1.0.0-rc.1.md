# Release run — v1.0.0-rc.1

**Tag commit:** PENDING — Phase C / squash-merge to main not yet executed
**Branch:** feat/spfx-1.22-heft-migration → main (PENDING)
**Run date:** 2026-05-02
**Operator:** Hemant Mane
**Phase:** A (automated steps 1-5); B + C deferred to user-driven session

## Pre-flight

- [x] Working tree clean (`git status --short` empty)
- [x] Node 22.14.0 verified (`node --version`)
- [x] On feat/spfx-1.22-heft-migration

## Steps

| # | Step | Outcome | Evidence |
|---|------|---------|----------|
| 1 | `npm install` | PASS — node_modules already populated; package-lock.json clean | No re-install required; tree clean per `git status --short` |
| 2 | `npm run type-check` | SKIP — using workaround per Found.D3 deferral | `npx tsc --noEmit -p tsconfig.json` exit 0; clean type-check, no output |
| 3 | `npm test` | PASS | 65 specs passed across 6 suites; 0 failures; exit 0; gated on Found.D13 (commit e062c52). Suites: Registry (19), configValidation (19), pnpPropertyControlsFix (3), lifecycle (2), SearchCoverageService (9), storeRegistry (13) |
| 4 | `npm run package` | PASS | `sharepoint/solution/sp-search.sppkg` produced; 2,630,523 bytes (2.6 MB); SHA-256 `ee0a101663e7ac5a9585482e145ae2f65a84e457a938dd3782c10c2e7f68e527` |
| 5 | `npm run check:bundles` | PASS (with concern, documented below) | All 6 web parts within budget per Found.D7 (commit 38bded6); 3.3-4.3% of budget when measured against the actual hashed asset filenames in `release/assets/`. Manual verification table below. **Gate script defect:** `scripts/check-bundle-sizes.js` looks up unhashed names (e.g. `sp-search-filters-web-part.js`) but Heft production builds emit content-hashed names (e.g. `sp-search-filters-web-part_2349af86f4f20f8a46c7.js`), so the script exits 2 with `missing asset`. Recommend a follow-up Foundations ticket (Found.D7 fix-up or D8 wiring task) to make the gate hash-aware before Phase C squash. |
| 6 | Tenant upload smoke | **PENDING — manual tenant smoke required (Phase B)** | Requires human-authenticated tenant access at https://pixelboy.sharepoint.com/sites/SPSearch |
| 7 | Multi-context smoke | **PENDING — manual tenant smoke required (Phase B)** | Same blocker as Step 6 |

## Step 5 manual verification (hash-aware budget check)

Source: `node -e` script that resolves each budget key via stem-prefix match against `release/assets/` listing. Output:

```
[PASS] sp-search-filters-web-part.js       -> sp-search-filters-web-part_2349af86f4f20f8a46c7.js:        862,498 bytes (budget 22,128,121,  3.9%)
[PASS] sp-search-results-web-part.js       -> sp-search-results-web-part_0699be3d5d6cc52e47c6.js:        714,103 bytes (budget 21,754,819,  3.3%)
[PASS] sp-search-admin-manager-web-part.js -> sp-search-admin-manager-web-part_4d0c439e05a21eea582b.js:  592,306 bytes (budget 16,684,972,  3.5%)
[PASS] sp-search-manager-web-part.js       -> sp-search-manager-web-part_7a16a2aa754e3e4b6b48.js:        591,819 bytes (budget 16,675,431,  3.5%)
[PASS] sp-search-box-web-part.js           -> sp-search-box-web-part_338d818760a646c13983.js:            546,789 bytes (budget 12,733,464,  4.3%)
[PASS] sp-search-verticals-web-part.js     -> sp-search-verticals-web-part_914bcf7c912b48cc546e.js:      470,158 bytes (budget 11,934,904,  3.9%)
Total breaches: 0
```

Note: current actual sizes (~470KB - 862KB) are dramatically smaller than the Found.D7 baseline (`docs/performance/bundle-sizes-baseline.json`, ~7.9MB - 14.7MB) because the production build is minified end-to-end. The baseline appears to have been measured against a non-minified or pre-minification artifact. The 1.5x budget ceilings still apply and are not breached.

## Phase split rationale

Phase A (this run) executes the automated, deterministic checklist steps that a subagent can drive end-to-end without human-authenticated environment access. Phase B (tenant upload + multi-context) and Phase C (squash-merge to main + tag + GitHub Release) require human-driven actions and are deferred to a follow-up user-driven session.

When Phase B is run, append outcomes to this file by filling Steps 6-7. When Phase C is run, fill the Tag commit field at the top.

## Concerns surfaced for Phase B/C operator

1. **Bundle-gate hash mismatch (medium):** `scripts/check-bundle-sizes.js` exits 2 against the current production build because it looks up unhashed filenames. Either (a) make the gate hash-aware (find by stem-prefix match), or (b) emit unhashed copies alongside hashed ones, before Phase C tags `v1.0.0-rc.1`. Otherwise CI will fail on the squash commit when Found.D8 wiring lands.
2. **Baseline magnitude vs current (informational):** Actual production bundle sizes are ~95% smaller than the Found.D7 baseline. Worth a sanity check on whether the baseline numbers were captured from a non-production or non-minified artifact and should be re-snapshotted before promoting to a long-lived budget.

## Phase A commit

This run log was committed alongside `docs/release-smoke-checklist.md` and the narrow CLAUDE.md edits (lines 7/48/115) in commit `<filled in post-commit>`.
